# Parameters for the GW role, substituted by envsubst into gw.cfg.tmpl.
# Inter-subnet routing, a single FRR router gateways two /24 subnets (without OSPF).
# PC1 (192.168.1.0/24) — eth0 R1 eth1 — PC2 (192.168.2.0/24).
FRR_HOSTNAME=GW
FRR_ETH0_IP=192.168.1.1/24
FRR_ETH1_IP=192.168.2.1/24
