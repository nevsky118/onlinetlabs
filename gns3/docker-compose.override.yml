# TEMP (to be removed): drop the 5433 host publication, it conflicts with another project's openalex-pg.
# gns3-service reaches postgres over internal DNS, the host port is not needed.
services:
  postgres:
    ports: !reset []
